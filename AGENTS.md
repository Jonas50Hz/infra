## Agent skills

### Issue tracker

GitHub Issues is this repository's issue tracker; use `gh`. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the five canonical triage labels. See `docs/agents/triage-labels.md`.

### PoC delivery scope

This is a local PoC: deliver the requested happy path and focused proof first.
Handle only highly probable normal-use failures beyond that. The mandatory
boundaries and full scope are in `.github/copilot-instructions.md`.

### Domain docs

Single-context: use the root `CONTEXT.md` and `docs/adr/`. See `docs/agents/domain.md`.