# Provisioned Processors

This directory contains processors-repository-owned `processor-*` services.
`processor-frequency-scale` is the seeded fake-PMU frequency conversion
service and `processor-apparent-power` derives fake-PMU per-phase apparent
power. Do not place infrastructure services here.