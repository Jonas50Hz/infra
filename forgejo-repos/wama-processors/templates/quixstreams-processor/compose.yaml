services:
  processor-template:
    image: "${WAMA_APPLICATION_IMAGE_PREFIX:-wama.local/wama-applications}/processor-template:${WAMA_APPLICATION_IMAGE_TAG:-main}"
    build:
      context: ../..
      dockerfile: templates/quixstreams-processor/Dockerfile
    environment:
      PROCESSOR_CONFIG_PATH: /etc/wama/processor.yaml
    volumes:
      - type: bind
        source: ${PROCESSOR_TEMPLATE_CONFIG_SOURCE:-./processor.yaml}
        target: /etc/wama/processor.yaml
        read_only: true
    networks:
      - wama-infra
    restart: unless-stopped

networks:
  wama-infra:
    external: true
    name: ${WAMA_INFRA_NETWORK:-wama-infra}