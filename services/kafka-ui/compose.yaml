services:
  kafka-ui:
    image: provectuslabs/kafka-ui:v0.7.2
    depends_on:
      kafka-init:
        condition: service_completed_successfully
    env_file:
      - ./kafka-ui.env
    ports:
      - "8080:8080"
    restart: unless-stopped
